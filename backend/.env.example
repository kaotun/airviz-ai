# File .env của dự án được đặt ở thư mục GỐC (Finalterm/.env)
# Xem file Finalterm/.env.example để biết tất cả biến môi trường cần cấu hình.
#
# Lý do: docker-compose.yml đọc .env từ thư mục gốc (env_file: - .env)
# nên toàn bộ backend, frontend, DB đều dùng chung một file .env duy nhất.
